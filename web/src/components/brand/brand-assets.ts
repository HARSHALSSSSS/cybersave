/** Full Cybersave lockup (icon + wordmark + tagline). */
export const BRAND_LOGO_SRC = '/brand-logo.png';

/** Transparent lockup for light backgrounds (navbar, footer). */
export const BRAND_LOCKUP_SRC = '/brand-lockup.png';

/** Tighter navbar crop — icon + Cybersave wordmark only (no tagline). */
export const BRAND_LOCKUP_NAV_SRC = '/brand-lockup-nav.png';

/** Icon mark only — C + shield. */
export const BRAND_ICON_SRC = '/brand-icon.png';

export const BRAND_LOGO_ALT = 'Cybersave — Digital Services, Trusted Always';

export const BRAND_TAGLINE = 'Digital Services • Trusted Always';

export const HERO_SHIELD_SRC = '/hero-shield.png';

/** Full lockup aspect ratio (width / height). */
export const BRAND_LOCKUP_ASPECT = 782 / 276;

/** Navbar lockup aspect ratio (width / height). */
export const BRAND_LOCKUP_NAV_ASPECT = 782 / 252;
