# Cybersave Mobile — API configuration
#
# Primary config: app/config/env.ts
#   USE_HOSTED_API = true  → Production Render API (recommended for APK builds)
#   USE_HOSTED_API = false → Local backend (Metro / emulator / LAN)
#
# Deployed API (already set in env.ts):
#   https://cybersave-l972.onrender.com/api/v1
#
# For local dev on a physical device, set your PC's LAN IP in env.ts:
#   DEV_API_HOST_OVERRIDE = '192.168.1.42'
#
# Or use USB port forwarding:
#   adb reverse tcp:8000 tcp:8000
